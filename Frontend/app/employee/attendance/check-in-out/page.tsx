import { PageContainer } from '@/components/ui/page-container';

export default function CheckInOutPage() {
  return (
    <PageContainer
      title="Check In / Check Out"
      subtitle="Digital punch clock to record your shift start, break, and end times"
      badge="Punch Clock"
    />
  );
}
