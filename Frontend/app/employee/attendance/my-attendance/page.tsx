import { PageContainer } from '@/components/ui/page-container';

export default function MyAttendancePage() {
  return (
    <PageContainer
      title="My Attendance"
      subtitle="Monthly attendance summary, total working hours, and punctuality rate"
      badge="Attendance"
    />
  );
}
