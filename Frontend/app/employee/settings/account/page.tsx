import { PageContainer } from '@/components/ui/page-container';

export default function AccountSettingsPage() {
  return (
    <PageContainer
      title="Account Settings"
      subtitle="Security preferences, password change, and notification channels"
      badge="Account"
    />
  );
}
