import { PageContainer } from '@/components/ui/page-container';

export default function EmployeeSalarySlipsPage() {
  return (
    <PageContainer
      title="Salary Slips"
      subtitle="View and download PDF salary slips for past payment cycles"
      badge="Payslips PDF"
    />
  );
}
