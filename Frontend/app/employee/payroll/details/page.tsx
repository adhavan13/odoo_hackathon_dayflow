import { PageContainer } from '@/components/ui/page-container';

export default function SalaryDetailsPage() {
  return (
    <PageContainer
      title="Salary Details"
      subtitle="Detailed breakdown of base salary, allowances, bonuses, and tax deductions"
      badge="Details"
    />
  );
}
