import { PageContainer } from '@/components/ui/page-container';

export default function AttendanceAnalyticsPage() {
  return (
    <PageContainer
      title="Attendance Analytics"
      subtitle="Punctuality, overtime hours, and absence pattern metrics"
      badge="Attendance AI"
    />
  );
}
