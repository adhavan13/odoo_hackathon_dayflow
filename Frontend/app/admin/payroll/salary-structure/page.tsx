import { PageContainer } from '@/components/ui/page-container';

export default function SalaryStructurePage() {
  return (
    <PageContainer
      title="Salary Structure"
      subtitle="Pay grades, allowances, deductions, and tax tier configurations"
      badge="Structure"
    />
  );
}
