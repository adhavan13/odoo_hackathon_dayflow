import { PageContainer } from '@/components/ui/page-container';

export default function AttendanceReportsPage() {
  return (
    <PageContainer
      title="Attendance Reports"
      subtitle="Exportable monthly and custom date range attendance summaries"
      badge="Reports"
    />
  );
}
