import { PageContainer } from '@/components/ui/page-container';

export default function OrganizationSettingsPage() {
  return (
    <PageContainer
      title="Organization Settings"
      subtitle="Company profile, work schedules, holidays, and system defaults"
      badge="Settings"
    />
  );
}
