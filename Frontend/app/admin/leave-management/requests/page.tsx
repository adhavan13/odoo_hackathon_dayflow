import { PageContainer } from '@/components/ui/page-container';

export default function LeaveRequestsPage() {
  return (
    <PageContainer
      title="Leave Requests"
      subtitle="Pending leave applications requiring approval or review"
      badge="Approvals"
    />
  );
}
